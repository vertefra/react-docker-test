sudo: request
services:
  - docker

before_install:
  - docker build -t dockerimage -f Dockerfile.dev .

# script that can be runned by travis on our container

script:
  - docker run -e CI=true dockerimage npm run test -- --coverage
